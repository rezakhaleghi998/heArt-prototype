services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: heart
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - heart-postgres:/var/lib/postgresql/data

volumes:
  heart-postgres:
